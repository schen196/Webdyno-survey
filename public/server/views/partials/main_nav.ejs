 <!-- Main Navigation -->
 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a href="/"><img src="\assets\images\webdyno.png" alt="Logo" class="img-fluid" width="80" height="80"</a>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">

          <!--Active Navigation-->
          <li class="nav-item">
            <a class=
            <% if (title == "Home") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/home">
              <i class="fas fa-lg fa-home"></i> Home</a>
          </li>

          <li class="nav-item">
            <a class=
            <% if (title == "Create") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/surveys/create">
              <i class="fa fa-plus-square-o"></i> Create a Survey</a>
          </li>

          <li class="nav-item">
            <a class=
            <% if (title == "Surveys") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/surveys">
              <i class="fa fa-question-circle-o"></i> Available Surveys</a>
          </li>

          <% if(typeof displayName=='string' && displayName) { %>
            <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %>
            </li>
            <li class="nav-item"><a href="/logout" class="nav-link">
                <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
            <% } else { %>
              <li class="nav-item"><a href="/login" class="nav-link">
                  <i class="fas fa-lg fa-sign-in-alt"></i> Login</a>
              </li>
              <% } %>
      </div>
    </div>
  </nav>